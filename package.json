{
  "name": "civic-report",
  "private": true,
  "version": "1.0.0",
  "description": "Accessible public-sector issue reporting portal — test-first showcase",
  "workspaces": [
    "server",
    "web",
    "e2e"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server & npm run dev --workspace=web",
    "build": "npm run build --workspace=server && npm run build --workspace=web",
    "test": "npm run test --workspace=server && npm run test --workspace=web",
    "test:e2e": "npm run test --workspace=e2e",
    "lint": "npm run typecheck --workspace=server && npm run typecheck --workspace=web"
  },
  "engines": {
    "node": ">=20"
  }
}
